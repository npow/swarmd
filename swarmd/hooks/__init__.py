"""Python hook entry points for the swarm harness (Claude Code hooks).

Shell hooks for regression tracking live alongside this module; they are
invoked by the Claude Code harness directly and don't depend on this
Python package.
"""
